name: cheap_learning
channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.8
  - pip=22.2

  - pip:
    - transformers[sentencepiece]
    - torch
    - openprompt
    - pygithub
    - pylint
    - emoji==2.2.0
    
  # Core python
  - numpy
  - tqdm
  - pandas
  - ipykernel
  - requests

  # ML
  - scikit-learn
  
  # Plotting
  - matplotlib
  - plotly
  - seaborn
